#!/bin/sh
# Gradle wrapper script
set -e
if command -v gradle >/dev/null 2>&1; then
    exec gradle "$@"
else
    echo "Gradle not found in PATH. Please install gradle or run through Android Studio."
    exit 1
fi
